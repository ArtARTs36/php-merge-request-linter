deptrac:
  paths:
    - src/

  layers:

    - name: 'Infrastructure'
      collectors:
        - type: directory
          value: src/Infrastructure/.*

    - name: 'Domain'
      collectors:
        - type: directory
          value: src/Domain/.*

  ruleset:
    Domain: ~
    Infrastructure: [ Domain ]
