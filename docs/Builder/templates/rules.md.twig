# Validation rules

Currently is available that rules:

| Name | Description |
| ------------ | ------------ |
{% for rule in rules %}
| {{ rule.name }} | {{ rule.description }} |
{% endfor %}

## Global parameters

| Name     | Description                                                                                                                          | Type    |
|----------|--------------------------------------------------------------------------------------------------------------------------------------|---------|
| critical | when `critical = true` the pipeline will fall <br/> when `critical = false` the pipeline will not fall, the error will be suppressed | boolean |
| when     | conditions for triggering the rule                                                                                                   | object  |

{% for rule in rules %}

## {{ rule.name }}

{{ rule.description }}

{% if rule.params.isEmpty == false %}
{% if rule.has_params_examples %}
| Name | Description | Type | Examples |
| ------------ | ------------ |------ | ------|
{% for param in rule.params %}
| {{ param.name }} | {{ param.description }} | {{ param.type }} {% if param.generic %} of {{ param.generic }}s {% endif %} {% if param.isGenericObject %} <br/> See details in Config JSON Schema{% endif %} | {% for example in param.examples %} {{ example }}{% if loop.last == false %},{% endif%} {% endfor %} |
{% endfor %}
{% else %}
| Name | Description | Type |
| ------------ | ------------ |------ |
{% for param in rule.params %}
| {{ param.name }} | {{ param.description }} | {{ param.type }} {% if param.generic %} of {{ param.generic }}s {% endif %} {% if param.isGenericObject %} <br/> See details in Config JSON Schema{% endif %} |
{% endfor %}
{% endif %}
{% endif %}
{% endfor %}
